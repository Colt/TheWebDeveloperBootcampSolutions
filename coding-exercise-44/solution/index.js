const square = function (num) {
  return num * num;
}

// More approaches:

// #2
const square = function (num) {
  return Math.pow(num, 2);
}

// #3 (doesn't work on the Udemy coding exercise platform at the time of writing this)
const square = function (num) {
  return num ** 2;
}